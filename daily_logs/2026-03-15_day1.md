# Day 1 | 2026-03-15 | NumPy实现MLP与反向传播

---

## 一、今日投入

| 项目 | 内容 |
|:---|:---|
| 开始时间 | 11:15 |
| 结束时间 | 12:05 |
| 净专注时长 | 1.5小时 |
| 学习模式 | ⚖️ 平衡模式 |

---

## 二、任务清单

| 任务 | 状态 |
|:---|:---:|
| 创建项目文件夹结构 | ✅ |
| NumPy实现MLP解决XOR问题 | ✅ |
| 理解前向传播与反向传播原理 | ✅ |
| 保存训练过程截图 | ✅ |

---

## 三、核心原理（类比+专业）

### 3.1 前向传播（Forward Propagation）

**生动类比：快递分拣系统**
- 输入层：包裹（X=[0,0]表示"小包裹+非易碎"）
- 隐藏层：分拣员（4个工人，各自判断"是否大件""是否易碎"）
- 输出层：最终决策（0=陆运，1=空运）
- 权重W：工人经验（大件专家 vs 易碎专家）
- 激活函数：工人决策强度（Sigmoid=0-100%的确定程度）

**专业解释**
当前层输入 = 上层输出 × 权重矩阵 + 偏置，再经激活函数非线性变换。
公式：z = W·a + b，a = σ(z)

---

### 3.2 Sigmoid激活函数

**生动类比：决策犹豫度**
- 输入很大正数 → 100%确定（接近1）
- 输入很大负数 → 0%确定（接近0）
- 输入接近0 → 犹豫期（0.5附近，最敏感）

**为什么用Sigmoid？**
- 输出0-1可解释为概率
- 导数可用自身表示：σ' = σ(1-σ)，计算省资源

**专业解释**
σ(x) = 1/(1+e^(-x))，将实数域映射到(0,1)，实现非线性。导数推导：
dσ/dx = e^(-x)/(1+e^(-x))^2 = σ(x)(1-σ(x))

---

### 3.3 反向传播（Backpropagation）

**生动类比：成绩复盘会**
- 期末考试(output)错了 → 算总分差多少（误差）
- 回溯：哪科老师教错了？（输出层权重梯度）
- 再回溯：哪个知识点没讲透？（隐藏层误差回传）
- 调整：下次备课重点改哪里（权重更新）

**专业解释**
链式法则的矩阵实现。输出层误差δ^L = (a^L-y)⊙σ'(z^L)，隐藏层δ^l = ((W^(l+1))^T δ^(l+1))⊙σ'(z^l)，权重更新ΔW = η·a^(l-1) δ^l

---

### 3.4 维度检查（Debug速查表）

| 张量 | 维度 | 含义 |
|:---|:---|:---|
| X | (4,2) | 4个样本，2维特征 |
| W1 | (2,4) | 输入2→隐藏4 |
| hidden | (4,4) | 4样本×4神经元 |
| W2 | (4,1) | 隐藏4→输出1 |
| output | (4,1) | 4样本×1输出 |

**记忆口诀**：(样本数, 特征数) @ (特征数, 神经元数) = (样本数, 神经元数)

---

## 四、必须掌握

| 知识点 | 自评 |
|:---|:---:|
| 能手推Sigmoid导数 | ⭐⭐⭐ |
| 理解链式法则在网络中的应用 | ⭐⭐⭐ |
| 能口算维度变化 | ⭐⭐⭐ |
| 解释XOR为何需隐藏层（线性不可分） | ⭐⭐⭐ |
| 梯度消失/爆炸概念 | ⭐⭐ |

---

## 五、错误记录

| 错误 | 现象 | 原因 | 解决 |
|:---|:---|:---|:---|
| 文件后缀 | python命令找不到文件 | 实际为day1_mlp.py.txt | 显示扩展名后重命名 |
| 缺少库 | ModuleNotFoundError | 未安装matplotlib | pip install安装成功 |

---

## 六、代码存档

**位置**：`day1_mlp.py`（项目根目录）

**验证方法**：
1. 复制下方代码到新文件`test_day1.py`
2. CMD运行：`python test_day1.py`
3. 验证输出：
   - 损失值从~0.25逐渐降到&lt;0.01
   - 最终预测接近[[0],[1],[1],[0]]

```python
import numpy as np

def sigmoid(x): return 1/(1+np.exp(-x))
def sigmoid_derivative(x): return x*(1-x)

class MLP:
    def __init__(self, i, h, o, lr=0.5):
        self.lr, self.W1, self.b1 = lr, np.random.randn(i,h)*np.sqrt(2/i), np.zeros((1,h))
        self.W2, self.b2 = np.random.randn(h,o)*np.sqrt(2/h), np.zeros((1,o))
    
    def forward(self, X):
        self.h = sigmoid(np.dot(X,self.W1)+self.b1)
        return sigmoid(np.dot(self.h,self.W2)+self.b2)
    
    def backward(self, X, y, out):
        d2 = (y-out)*sigmoid_derivative(out)
        self.W2 += self.lr*np.dot(self.h.T,d2)
        d1 = np.dot(d2,self.W2.T)*sigmoid_derivative(self.h)
        self.W1 += self.lr*np.dot(X.T,d1)
        return np.mean((y-out)**2)

# 验证代码
X,y = np.array([[0,0],[0,1],[1,0],[1,1]]), np.array([[0],[1],[1],[0]])
mlp = MLP(2,4,1)
print("开始训练XOR问题...")
for i in range(10000):
    loss = mlp.backward(X,y,mlp.forward(X))
    if i%2000==0: print(f"Epoch {i:5d}, Loss: {loss:.6f}")
pred = mlp.forward(X)
print(f"\n最终预测:\n{pred.round()}")
print(f"目标值:\n{y}")
print(f"准确率: {np.mean((pred.round()==y)*100):.0f}%")
input("按回车键退出...")
```

## 七、训练结果

运行 `python test_day1.py` 输出：
开始训练XOR问题...
Epoch 0, Loss: 0.253874
Epoch 2000, Loss: 0.004928
Epoch 4000, Loss: 0.001707
Epoch 6000, Loss: 0.001014
Epoch 8000, Loss: 0.000717

最终预测:
[[0.]
[1.]
[1.]
[0.]]
目标值:
[[0]
[1]
[1]
[0]]
准确率: 100%


## 八、训练曲线

![训练曲线](../../results/beautified/day1_training_curves.png)

## 九、文件列表
```
项目根目录
├── daily_logs/
│ └── 2026-03-15_day1.md
├── day1_mlp.py
├── day1_mlp_pytorch.py
├── test_day1.py
├── day1_training_curves.png
├── README.md
└── requirements.txt
```

## 十、运行方法

```bash
# 安装依赖
pip install numpy matplotlib

# 运行NumPy版本
python day1_mlp.py

# 运行测试文件
python test_day1.py
```

## 十一、疑问清单

| 问题 | 优先级 | 计划解决 |
|:---|:---:|:---|
| PyTorch的autograd底层实现？ | 中 | Day 2 |
| 隐藏层神经元数量选择依据？ | 低 | 周末实验 |
| He初始化与Xavier初始化区别？ | 低 | Week 2 |

## 十二、复盘

| 项目 | 内容 |
|:---|:---|
| 最大收获 | 亲手实现反向传播，理解"梯度"是具体矩阵运算 |
| 明日关键 | 对比PyTorch的autograd如何实现自动求导 |
| 当前状态 | 平衡完成，无疲劳感 |

## 十三、GitHub问题与解决记录

### 1. 在错误目录初始化仓库
**现象**：在 `C:\Users\34059\` 用户目录误执行 `git init`

**解决**：
```bash
rm -rf .git
cd /d/Desk/desk/beiyou_c_project
git init
git add .
git commit -m "Day 1: 完成MLP实现"
```

### 2. SSH连接超时（22端口被屏蔽）
**解决**：配置SSH使用443端口，编辑 `~/.ssh/config`：

```ssh-config
Host github.com
    Hostname ssh.github.com
    User git
    Port 443
    PreferredAuthentications publickey
    IdentityFile ~/.ssh/id_rsa
```

### 3. SSH密钥生成卡住
**现象**：停在 `Enter file in which to save the key`

**解决**：按回车接受默认路径，后续步骤：

```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub
```

### 4. 首次Push被拒绝
**现象**：`! [rejected] main -> main (fetch first)`

**解决**：

```bash
git push -u origin main --force
```

### 5. GitHub页面加载异常
**现象**：显示 "Error loading page"

**解决**：服务器临时问题，等待后刷新

### 6. Markdown格式规范

| 错误类型 | 错误示例 | 正确写法 |
|:---|:---|:---|
| 标题缺少空格 | `##八、` | `## 八、` |
| 代码块未闭合 | 单引号包裹 | 三个反引号包裹 |
| 图片路径错误 | `./images/xx.png` | `../../results/beautified/images/xx.png` |
| 表格分隔符不齐 | 空格不一致 | 保持 `\|` 对齐 |

---

## 操作检查清单

- [ ] 执行git命令前 `pwd` 确认在项目根目录
- [ ] Commit前 `git status` 检查待提交文件
- [ ] 网络超时优先尝试443端口SSH配置
- [ ] Markdown表格使用标准 `\|` 分隔符并保持对齐
```